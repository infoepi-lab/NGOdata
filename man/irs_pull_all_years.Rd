% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_run_pipeline.R
\name{irs_pull_all_years}
\alias{irs_pull_all_years}
\title{One-call complete data pull across all years with timestamped report}
\usage{
irs_pull_all_years(
  eins = character(),
  ein_file = NULL,
  years = 2017:2025,
  output_dir = NULL,
  project_root = NULL
)
}
\arguments{
\item{eins}{Character vector of 9-digit EINs (no dashes required).}

\item{ein_file}{Optional path to a text file (one EIN per line, \code{#} comments ok).}

\item{years}{Integer vector of tax years (default \code{2017:2025}).}

\item{output_dir}{Optional override for the output directory. Defaults to
\code{paths$output_dir} from \code{\link[=irs_project_paths]{irs_project_paths()}}.}

\item{project_root}{Repo root for \code{\link[=irs_project_paths]{irs_project_paths()}}.}
}
\value{
Invisibly returns a list with \code{result} (the \code{\link[=run_irs990_pipeline]{run_irs990_pipeline()}}
return value) and \code{report_path} (absolute path to the timestamped report).
}
\description{
Convenience wrapper around \code{\link[=run_irs990_pipeline]{run_irs990_pipeline()}} that fetches, parses,
and analyzes filings for the requested EINs across the full default year
range, then writes a Markdown summary report into the output directory.
The report filename embeds the run start time so repeated pulls do not
overwrite each other.
}
\examples{
\dontrun{
# Pull every available year for two organizations and write a report
irs_pull_all_years(eins = c("237404756", "131923701"))

# Pull from a file of EINs into a custom output directory
irs_pull_all_years(ein_file = "eins.txt", output_dir = "output/run-2026")
}
}
