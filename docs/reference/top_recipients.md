# Find top grant recipients

Identifies the organizations or individuals receiving the largest total
grant amounts across all years.

## Usage

``` r
top_recipients(df, n = 20L)
```

## Arguments

- df:

  Data frame containing foreign grants data

- n:

  Number of top recipients to return (default 20)

## Value

Tibble with top grant recipients and their summary statistics
