# Find top compensated board members

Returns the highest compensated individuals from board member data.

## Usage

``` r
top_compensated(df, n = 20L)
```

## Arguments

- df:

  Data frame containing board member data with compensation

- n:

  Number of top compensated individuals to return (default 20)

## Value

Tibble with top compensated board members
