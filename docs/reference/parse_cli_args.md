# Parse CLI args (mirrors `scripts/run_analysis.py` options)

Parse CLI args (mirrors `scripts/run_analysis.py` options)

## Usage

``` r
parse_cli_args(argv = commandArgs(trailingOnly = TRUE))
```

## Arguments

- argv:

  Character vector (default: `commandArgs(trailingOnly = TRUE)`)
