# XPath variants for Schedule F Part I "type of activities conducted" (same list as `FOREIGN_ACTIVITY_FIELDS$activities`).

XPath variants for Schedule F Part I "type of activities conducted"
(same list as `FOREIGN_ACTIVITY_FIELDS$activities`).

## Usage

``` r
irs_xpaths_type_of_activities_conducted()
```
