# Generate regional heatmap data

Creates a wide-format data frame suitable for heatmap visualization
showing grant amounts by region and year.

## Usage

``` r
regional_heatmap_data(df)
```

## Arguments

- df:

  Data frame containing foreign grants data

## Value

Tibble with grant amounts by region (rows) and year (columns)
