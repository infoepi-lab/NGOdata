# Analyze grant purposes

Groups grants by purpose and calculates total amounts and counts for
each purpose category.

## Usage

``` r
purpose_categories(df)
```

## Arguments

- df:

  Data frame containing foreign grants data with purpose field

## Value

Tibble with grant amounts and counts by purpose, ordered by amount
